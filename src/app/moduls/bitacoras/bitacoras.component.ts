import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-bitacoras',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './bitacoras.component.html',
})
export default class BitacorasComponent {}
