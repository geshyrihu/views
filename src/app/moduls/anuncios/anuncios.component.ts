import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-anuncios',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './anuncios.component.html',
})
export default class AnunciosComponent {}
